import connect from "react-redux"
import ChooseHaircut from "../choose_haircut/choose_haircut"
    
const msp = (state) => {
    return(
        // images: state.entities.haircuts
    )
}

const mdp = (dispatch) => {
    return(
        
    )
}

export default connect(msp,mdp)(ChooseHaircut)

